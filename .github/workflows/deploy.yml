name: Deploy to EC2

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: 65.2.11.121
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e

            cd /home/ubuntu

            if [ ! -d "b86b6270-37f3-41cd-acb3-f213f3750249" ]; then
              git clone https://github.com/${{ github.repository }}.git
            fi

            cd $(basename $(git rev-parse --show-toplevel) 2>/dev/null || echo "b86b6270-37f3-41cd-acb3-f213f3750249")

            git pull origin main

            docker stop app || true
            docker rm app || true
            docker rmi app || true

            docker build -t app .
            docker run -d -p 80:3000 --name app app
